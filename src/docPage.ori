// A single page in the docs area; uses the base page template
(markdown, fileName) => page.ori.html({
  title: fileName
  nav: "nav goes here"
  @text: marked.js(markdown) // Convert markdown to HTML for the page body
})
